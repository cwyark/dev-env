// Managed by chezmoi.
//
// Replace this with your real zellij config after deciding which settings are
// portable across macOS and remote Linux.

default_shell "{{ .chezmoi.env.SHELL | default "/bin/sh" }}"
simplified_ui false

keybinds {
    normal {
        bind "Ctrl g" { SwitchToMode "Locked"; }
    }
}
